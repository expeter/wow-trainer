import { createPlannedEncounterPackage } from '../../platform/encounters/plannedPackage'
export default createPlannedEncounterPackage({ id: 'ulatek', name: "Ula'tek", order: 8, summary: 'The final encounter remains research-only until live evidence is available.' })
